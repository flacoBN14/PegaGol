{
  "name": "pegagol",
  "version": "1.0.0",
  "description": "Album de estampas del Mundial FIFA 2026",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npx prisma generate && npx prisma migrate deploy && node prisma/seed.js",
    "start": "cd server && node src/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
